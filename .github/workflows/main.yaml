name: first CI
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World!"
      - name: List all files
        run: ls -l 
      - uses: actions/checkout@v5 
      - run: ls -l